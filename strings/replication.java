package strings;

public class replication {
    public static void main(String[] args) {
        String name = "Sarvesh";

        // .charAT() function is used for string replication
        for (int i = 0; i < name.length(); i++) {
            System.out.println(name.charAt(i));            
        }
    }
}
